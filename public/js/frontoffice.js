// Generated by CoffeeScript 1.12.7
(function() {
  $.ajaxSetup({
    headers: {
      'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
    }
  });

  $(document).ready(function() {
    var getUrlVars, requiredInvestment;
    getUrlVars = function() {
      var hash, hashes, i, vars;
      vars = [];
      hash = void 0;
      hashes = window.location.href.slice(window.location.href.indexOf('?') + 1).split('&');
      i = 0;
      while (i < hashes.length) {
        hash = hashes[i].split('=');
        vars[hash[0]] = hash[1];
        $('td[data-search="' + hash[0] + '"]').find('.datatable-search').val(hash[1]);
        i++;
      }
      return vars;
    };
    $(document).on('click', '.btn-invest-now', function(e) {
      var business_id, business_type, user_id;
      e.preventDefault();
      business_type = $(this).attr('data-businesstype');
      user_id = $(this).attr('data-userid');
      business_id = $(this).attr('data-businessid');
      return $.ajax({
        type: 'post',
        url: '/frontoffice/show-invest-in-business-modal',
        headers: {
          'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
        },
        data: {
          'business_type': business_type,
          'business_id': business_id,
          'user_id': user_id
        },
        success: function(data) {
          $('#cust-invest-modal').remove();
          $('body').append(data.invest_modal);
          return $('#cust-invest-modal').modal('show');
        }
      });
    });
    $(document).on('click', '#btn_investform', function(e) {
      var format_max_investment;
      e.preventDefault();
      if ($('#max_inv_planned').val() === '') {
        $('#investment_msg').html("Please enter Investment amount.");
        $('#investment_msg').addClass('text-danger');
        return setTimeout(function() {
          $('#investment_msg').html("");
          return $('#investment_msg').removeClass('text-danger');
        }, 5000);
      } else if ($('#max_inv_planned').val() > 0) {
        $('#investment_msg').html("");
        $('#investment_msg').removeClass('text-danger');
        format_max_investment = formatAmount($('#max_inv_planned').val(), 2, true, true);
        return $('#invest-confirm').modal('show');
      }
    });
    $(document).on('click', '#btn_cancel_conf_investment', function(e) {
      e.preventDefault();
      return $('#invest-confirm').modal('hide');
    });
    return requiredInvestment = function() {
      var desired_investment, price_per_share;
      desired_investment = $('#pledge_desiredinvestment').val()(",", "");
      return price_per_share = $('#pledge_pricepershare').val().replace(",", "");
    };
  });

}).call(this);
